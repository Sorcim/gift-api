# Surcharge production
# Usage : docker compose -f compose.yaml -f compose.production.yaml up -d
# Les valeurs REPLACE_ME sont à définir dans .env.production.local (gitignored)
services:
  api:
    build:
      target: production
    image: ghcr.io/${GITHUB_OWNER}/gift-api:latest
    environment:
      APP_ENV: prod
      APP_DEBUG: "0"
      SERVER_NAME: ${SERVER_NAME}
      APP_SECRET: ${APP_SECRET}
      DATABASE_URL: ${DATABASE_URL}
      SESSION_DSN: ${SESSION_DSN}
      CORS_ALLOW_ORIGIN: ${CORS_ALLOW_ORIGIN}
      RESEND_API_KEY: ${RESEND_API_KEY}
      RESEND_WEBHOOK_SECRET: ${RESEND_WEBHOOK_SECRET}
    volumes: []
    restart: always

  frontend:
    image: ghcr.io/${GITHUB_OWNER}/gift-frontend:latest
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_URL: https://${SERVER_NAME}
      NEXT_API_URL: http://api
    volumes: []
    restart: always

  db:
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    restart: always
